class SchoolRule {
  final int id;
  final String title;
  final String description;
  final String createdAt;

  const SchoolRule({
    required this.id,
    required this.title,
    required this.description,
    required this.createdAt,
  });
}